<div class="ready">
    <div class="container">
        <div class="ready__info wow slideInUp">
            <h2 class="ready__title">{{title}}</h2>
            <p class="ready__desc">{{desc}}</p>
            <a href="#" class="button--blue">{{link}}</a>
        </div>
    </div>
</div>
