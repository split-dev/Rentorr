<div class="apply">
    <div class="apply__dots-position"></div>
    <div class="container">
        <div class="row align-content-center flex-row-reverse">
            <div class="col-lg-6 apply__title-position">
                <h3 class="apply__title">{{title}}</h3>
                <img src="{{img}}" alt="img">
            </div>
            <div class="col-lg-6 apply__text-box-position">
                <div class="apply__text-box">
                    <h3 class="apply__title">{{title}}</h3>
                    <p class="apply__text">{{text}}</p>
                    <a href="#" class="apply__link">Learn more</a>
                </div>
            </div>
        </div>
    </div>
</div>
