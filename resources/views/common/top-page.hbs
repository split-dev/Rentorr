<div class="top-pages">
    <div class="container">
        <div class="row {{revers}} justify-content-between">
            <div class="col-lg-{{size-text}} top-pages__text">
                <h1>{{title}}</h1>
                <p>{{desc}}</p>
                <a href="{{link}}" class="nav__button--blue">{{link-name}}</a>
            </div>
            <div class="col-lg-{{size-img}}">
                <img src="{{images}}" alt="img">
            </div>
        </div>
    </div>
</div>
